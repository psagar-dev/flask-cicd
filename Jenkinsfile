pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Cloning code...'
                sh 'https://github.com/psagar-dev/flask-cicd.git'
            }
        }
    }
}
